{
  "name": "@webhook-auto/worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@webhook-auto/config": "workspace:*",
    "@webhook-auto/database": "workspace:*",
    "@webhook-auto/logger": "workspace:*",
    "@webhook-auto/security": "workspace:*",
    "@webhook-auto/types": "workspace:*",
    "axios": "^1.6.7",
    "bullmq": "^5.1.0",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
